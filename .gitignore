.DS_Store
.gstack/

# New-biz-prep 내부 산출물 — PUBLIC 레포 노출 금지 (수주 전략·가격·리서치 raw)
# index.html(브리프 정본)만 공개, _workspace/.raw는 로컬 보존
research/*/_workspace/
research/*/.raw/

# Vercel / node (인증 게이트 도입)
node_modules/
.vercel/
.env
.env.local
*.log
